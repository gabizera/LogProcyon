services:
  clickhouse:
    image: clickhouse/clickhouse-server:latest
    container_name: log-clickhouse
    environment:
      - CLICKHOUSE_USER=default
      - CLICKHOUSE_PASSWORD=
      - CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - ./clickhouse/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "127.0.0.1:8123:8123"
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    networks:
      - lognet
    healthcheck:
      test: ["CMD", "clickhouse-client", "--query", "SELECT 1"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  collector:
    image: ghcr.io/gabizera/logprocyon-collector:latest
    build:
      context: ./collector
    container_name: log-collector
    network_mode: host
    depends_on:
      clickhouse:
        condition: service_healthy
    environment:
      - CLICKHOUSE_URL=http://localhost:8123
      - LISTEN_PORT=514
      - BATCH_SIZE=100
      - FLUSH_INTERVAL=2000
      - TZ_OFFSET_HOURS=${TZ_OFFSET_HOURS:--3}
      - DATA_DIR=/data
    volumes:
      - logdata:/data
    restart: unless-stopped

  backend:
    image: ghcr.io/gabizera/logprocyon-backend:latest
    build:
      context: ./backend
    container_name: log-backend
    depends_on:
      clickhouse:
        condition: service_healthy
    environment:
      - CLICKHOUSE_URL=http://clickhouse:8123
      - DATA_DIR=/data
      - JWT_SECRET=${JWT_SECRET}
      - CORS_ORIGINS=${CORS_ORIGINS:-}
    volumes:
      - logdata:/data
    networks:
      - lognet
    restart: unless-stopped

  frontend:
    image: ghcr.io/gabizera/logprocyon-frontend:latest
    build:
      context: ./frontend
    container_name: log-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - lognet
    restart: unless-stopped

volumes:
  clickhouse_data:
    driver: local
  logdata:
    driver: local

networks:
  lognet:
    driver: bridge

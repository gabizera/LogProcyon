# Timezone offset em horas (ex: -3 para BRT, 0 para UTC, -4 para AMT)
TZ_OFFSET_HOURS=-3

# JWT secret — OBRIGATÓRIO, mínimo 32 caracteres
# Gere com: openssl rand -hex 32
JWT_SECRET=

# CORS origins (opcional, separado por vírgula)
# CORS_ORIGINS=https://meudominio.com.br
